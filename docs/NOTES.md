# 学习笔记（NOTES）

## 「拉」与「推」的差别

你用 `GET /posts` **拉**：你决定调用时机。Webhook **推**：平台决定何时呼叫你；你的服务要能快速 **2xx**，否则对方会判定失败并重试队列。

## 为什么默认 3050

很多人本机常驻 `3000`（Stage 3），把 Receiver 固定在 `3050` 减少心智拉扯；也可以统一用环境变量覆盖。

## 为什么要打印 body

初学阶段：**看见数据**比什么都重要；后面再升级到结构化日志、pino、OpenTelemetry。
